# Workout Text Allowed Subset

Project subset rules:
- every step line starts with `-`
- duration is always time-based
- target is always power-based percent or ramp percent
- cadence is always required
- distance targets, HR, pace, freeride, and absolute watts are not used in RPS workout text
- percent ranges must include `%` on both bounds
