# Loop and Section Grammar

Project subset for workout structure:
- section order is fixed by project convention
- loop headers use `Nx` and are separate from step lines
- nested loops are forbidden
- optional sections are omitted entirely when unused
- step lines always start with `-`
