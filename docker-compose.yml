version: "3.9"

services:
  rps:
    image: ghcr.io/OWNER/REPO:latest
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./runtime:/app/runtime
    restart: unless-stopped
